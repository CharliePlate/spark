{
  "name": "spark",
  "module": "index.ts",
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.2.2"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "tsc",
    "dev-watch": "tsc -w",
    "live-server": "live-server --port=8080 --entry-file=public/index.html"
  }
}
